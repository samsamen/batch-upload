[phases.install]
cmds = ['npm install --prefix backend && npm install --prefix frontend']

[phases.build]
cmds = ['npm run build --prefix frontend && cp -r frontend/dist backend/public']

[start]
cmd = 'node backend/server.js'
